# This file belongs in /etc/nginx/conf.d/, which Nginx includes from its http
# context. It intentionally lives outside a server block.
limit_req_zone $binary_remote_addr zone=geocontextualize_api:10m rate=12r/m;
limit_conn_zone $binary_remote_addr zone=geocontextualize_per_ip:10m;
